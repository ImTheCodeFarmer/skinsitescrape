{
  "$schema": "https://railway.app/railway.schema.json",
  "build": { "builder": "DOCKERFILE", "dockerfilePath": "apps/collector/Dockerfile" },
  "deploy": { "restartPolicyType": "ALWAYS", "numReplicas": 1 }
}

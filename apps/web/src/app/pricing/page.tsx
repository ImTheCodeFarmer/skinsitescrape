import { PricingView } from "@/components/views/pricing";

export const metadata = { title: "Pricing — SkinWagerTracker", description: "Full access to SkinWagerTracker: player profiles, cross-site tracking, a custom Telegram bot for alerts and custom feature requests." };

export default function Page() {
  return <PricingView />;
}
